#include <stdio.h>
#include <stdlib.h>
int main (void)
{
	int a;
	int b;
	int c;
	int d;
	int e;
	int f;
	int g;
	int h;
	int i;
	int j;
	
	int A=0;
	int B=0;
	int C=0;
	
	printf("½Ð¿é¤J¦¨ÁZ¡G");
	scanf("%d",&a);
	scanf("%d",&b);
	scanf("%d",&c);
	scanf("%d",&d);
	scanf("%d",&e);
	scanf("%d",&f);
	scanf("%d",&g);
	scanf("%d",&h);
	scanf("%d",&i);
	scanf("%d",&j);
	
	if (a>=76)
	A++;
	if  (a<=75 && a>=60)
	B++;
	if (a<=59 && a>=0)
	C++;
	if (b>=76)
	A++;
	if  (b<=75 && b>=60)
	B++;
	if (b<=59 && b>=0)
	C++;
	if (c>=76)
	A++;
	if  (c<=75 && c>=60)
	B++;
	if (c<=59 && c>=0)
	C++;
	if (d>=76)
	A++;
	if  (d<=75 && d>=60)
	B++;
	if (d<=59 && d>=0)
	C++;
	if (e>=76)
	A++;
	if  (e<=75 && e>=60)
	B++;
	if (e<=59 && e>=0)
	C++;if (f>=76)
	A++;
	if  (f<=75 && f>=60)
	B++;
	if (f<=59 && f>=0)
	C++;
	if (g>=76)
	A++;
	if  (g<=75 && g>=60)
	B++;
	if (g<=59 && g>=0)
	C++;
	if (h>=76)
	A++;
	if  (h<=75 && h>=60)
	B++;
	if (h<=59 && h>=0)
	C++;
	if (i>=76)
	A++;
	if  (i<=75 && i>=60)
	B++;
	if (i<=59 && i>=0)
	C++;if (j>=76)
	A++;
	if  (j<=75 && j>=60)
	B++;
	if (j<=59 && j>=0)
	C++;
	
	printf ("A=%d",A);
	printf ("B=%d",B);
	printf ("C=%d\n",C);
	
	system("pause");
	return 0;
}
